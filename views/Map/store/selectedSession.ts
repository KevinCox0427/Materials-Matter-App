import { createSlice } from "@reduxjs/toolkit";

type SelectedSession = number;

export const selectedSessionSlice = createSlice({
    name: 'selectedSession',
    initialState: 0,
    reducers: {
        
    }
});